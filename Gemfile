source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }
gem "nokogiri"
group :development, :test do 
    gem "pry"
end 

# Specify your gem's dependencies in NewestBookReleases.gemspec
gemspec
